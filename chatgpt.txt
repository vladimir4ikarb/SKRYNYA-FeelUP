1. Убери server.ts из архитектуры, оставь только Firebase (Firestore + Cloud Functions)

2. Запрети любые прямые изменения `currentStock` из UI и любых мест кроме ядра

3. Создай слой `src/core` и вынеси туда всю бизнес-логику из README

4. Реализуй функции:

   * createOrder
   * reserveStock
   * completeOrder
   * cancelOrder

5. Все операции со складом делай только через эти функции, без исключений

6. Убери hybrid inventory (никаких пересчётов “на лету”), оставь только `currentStock`

7. Везде добавь проверки перед списанием:

   * нельзя уйти в минус
   * нельзя списать без статуса

8. Все изменения делай через Firestore transactions (одна операция = одна транзакция)

9. При создании закупки фиксируй `costPrice` в PurchaseItem

10. При создании OrderItem сохраняй snapshot себестоимости (не брать из Product)

11. Перестань считать прибыль от текущего `costPrice`, считай только от snapshot

12. Раздели состояния заказа строго:

* processing → только резерв
* completed → списание + доход
* cancelled → снятие резерва

13. Добавь явное поле `reservedStock` у продукта

14. Логику резерва вынеси отдельно (не мешать с фактическим остатком)

15. Любое изменение статуса заказа должно идти через одну функцию (не напрямую)

16. Добавь логирование всех операций (create, status change, stock change)

17. Убери возможность удаления сущностей, если они участвуют в операциях (зашей это в код, не только в правила)

18. Проверь, чтобы ни один UI-компонент не писал напрямую в Firestore

19. Сделай один entry-point для всех записей в базу (через функции/сервисы)

20. Приведи структуру к виду:

* core (логика)
* services (работа с Firestore)
* ui (только отображение и вызов функций)

21. После этого пройтись по всем сценариям:

* создать заказ
* отменить
* выполнить
* проверить склад и финансы на каждом шаге

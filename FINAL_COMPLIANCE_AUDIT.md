# Final Compliance Audit

Дата: 2026-04-21

Цель проверки: подтвердить фактическое соответствие текущей реализации требованиям `BUSINESS_LOGIC.md` и `CORE_ARCHITECTURE.md` после выполненного плана.

## 1. BUSINESS_LOGIC.md

### 1.1 Складская модель (Actual/Reserved/Free)
- Статус: **Соответствует**.
- Подтверждение:
  - Реализован `useInventory` и чистая математика `calculateInventorySnapshot`.
  - `Actual` считает входы `Оплачено/Борг` и выходы `Виконано`.
  - `Reserved` считает `В обробці`.
  - `Free = Actual - Reserved`.

### 1.2 Транзакционная проверка свободного остатка
- Статус: **Соответствует**.
- Подтверждение:
  - Добавлен транзакционный расчёт `calculateFreeStockInTransaction`.
  - Проверка `Free Stock` встроена в ключевые write-сценарии для заказов/позиций.

### 1.3 copyOrder
- Статус: **Соответствует**.
- Подтверждение:
  - `copyOrder` переписан на одну атомарную транзакцию (all-or-nothing).
  - Цена фиксируется как `product.price` с fallback на старую.

### 1.4 UI-ограничения
- Статус: **Соответствует (по ключевому требованию)**.
- Подтверждение:
  - Убраны `document.getElementById` из Order/Purchase форм (переведено на `useRef`).

## 2. CORE_ARCHITECTURE.md

### 2.1 Header-Line Items
- Статус: **Соответствует**.
- Подтверждение: сохранена структура `orders/orderItems` и `purchases/purchaseItems`.

### 2.2 Денормализация цен
- Статус: **Соответствует**.
- Подтверждение:
  - Позиции сохраняют фиксированную цену (`price`/`costPrice`) и `total`.

### 2.3 Soft Delete
- Статус: **Соответствует**.
- Подтверждение:
  - Доменные удаления переведены в soft-delete.
  - Опасные hard-delete сценарии убраны из рабочих потоков.

### 2.4 Audit Log (write-only/immutable)
- Статус: **Соответствует**.
- Подтверждение:
  - В `firestore.rules`: `logs` разрешён только на create, update/delete запрещены.

### 2.5 Security Rules
- Статус: **Существенно усилено, соответствует целевой модели доступа**.
- Подтверждение:
  - Убраны глобальные permissive write-правила.
  - Введён role-based доступ (`viewer/manager/admin`) с валидацией.

## 3. Миграция и обратная совместимость
- Статус: **Выполнено**.
- Подтверждение:
  - Добавлен скрипт миграции и runbook.
  - Выполнены dry-run и execute на текущей БД.
  - На переходный период сохранены legacy fallback-поля.

## 4. Проверка качества
- Type check: `npm run lint` — успешно.
- Unit tests: `npm test` — успешно (3/3).

## Итог
- Текущий статус: **проект приведён в практическое соответствие требованиям документов** с рабочей миграцией, усиленной безопасностью и проверяемой складской логикой.
- Рекомендуемый next step: после стабилизации в проде удалить legacy fallback-поля и завершить финальную чистку схемы.
